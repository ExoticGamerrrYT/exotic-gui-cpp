@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# GLFW is linked privately into the static library, but the consumer still has
# to resolve its symbols at link time.
find_dependency(glfw3 QUIET)

include("${CMAKE_CURRENT_LIST_DIR}/ExoticGuiTargets.cmake")

check_required_components(ExoticGui)
